#include "main.h"
/**
 *_isdigit - function
 *@c: function character
 *Return: returns 0 if c is a digit, returns 0 if c is not
 */

int _isdigit(int c)
{

if ((c <= 0) && (c >= 9))

return (1);

return (0);
}
